MAX = 500;

hello = "world";
b = 'c';

function add1(i : int) : int
{
  a : int;
  b : int;
  a := 1;
  b := 2;
  a := add1(b); 
  return i+1;
}

procedure main(s : array of string)
{

  // Declarations
  m : map of (int, string);
  str : string;
  str2 : string;
  t : bool;
  a : int;
  b : int;
  c : int;
  d : int;
  j : int;

  // Basic instructions
  a := 452;
  b := 1;
  c := 0;
  t := true;
  str := "rr";
  str2 := str + "tt";

  // Undefined variables
   
  //Detected
  //c := u + 1;
  //z := 55;
  //c := add1(u, v);
  //c := add1(u);
  //add1(jimmy);
  //c := (u);
  //c := add1(t);
  //c := a + b + t;

  //TODO undetected
  //c := u.v;

  // Hashmap tests
  //m := {(d, "delta")};
  m := {(a, "alpha"), (b, "beta")};

  // Priority tests
  a := -(2 + (c/2 + a)) * b;

  // Function calls
  c := add1(a) + b; 
  //c := add1(bar) + b; 
  //t := add1(a);
  
  writeln("Finished");

}