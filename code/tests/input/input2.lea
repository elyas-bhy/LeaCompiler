MAX = 500;

hello = "world";
b = 'c';


Foo = struct {
  x : int;
  y : int;
  b : Bar;
}

Bar = struct {
  d : string;
}

function add1(i : int) : int
{
  return i+1;
}

procedure main(s : array of string)
{

  // Declarations
  m : map of (int, string);
  str : string;
  t : bool;
  a : int;
  b : int;
  c : int;

  a := 1;
}