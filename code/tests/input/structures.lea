Foo = struct {
  x : int;
  y : int;
  b : Bar;
}

Bar = struct {
  d : string;
}

procedure main(s : array of string) {
	a : int;
	b : int;
	f : Foo;
	f2 : Foo;
	bar : Bar;

	a := 1;
	b := 2;

	// Struct tests
	bar := Bar();
	//f := Foo();
	//f2 := f;
	f := Foo(a, b, bar);
	f.x := 4;		//OK
	//a := f.h;		//Detected
	//f.h := 4;		//Detected
	//f.b.u := 5;	//Detected
}
