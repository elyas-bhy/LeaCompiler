
procedure foo(i : int) {
	a : int;
	a := 1;
}

function bar(i : int, j : int) : int {
	a : int;
	a := 1;
	return i + a;
}

function bar2(i : int) : int {
	a : int;
	a := 1;
	return i + a;
}

procedure main(args : array of string) {
    a : int;
    b : int;
    z : string;
    c : array of int;


	a := 1;
	b := a;
    c := array of int[b];
    c[0] := 4;
    c[b] := b;
    b := c[b];

    foo(c[b]);
    //a := b + 1;       //Detected
    a := bar(b, 5);		//Detected
    a := bar2(5);
}